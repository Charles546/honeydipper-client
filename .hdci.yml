---
use_template: Charles546/honeydipper-client@refs/heads/test:hdci.tmpl

triggers:
  - conditions:
      gh_event: push
    stages:
      - test

stages:
  test:
    jobs:
      - test

jobs:
  test:
    steps:
      - secretDriverLoader
      - checkout
      - test
